${app}:
  image: java:8
  working_dir: /app
  volumes:
    - ./app:/app

  command: java -jar /app/${app}-${ver}.jar -XX:PermSize=30m -Xmx68m --spring.profiles.active=${profile}

  ports:
${portsArray}

  environment:
    HOSTNAME: vpclub

  extra_hosts:
    vpclub: ${hostIpAddress}

